import { Inder, Inter } from 'next/font/google';

export const INDER = Inder({
  subsets: ['latin'],
  weight: ['400'],
  style: 'normal',
  variable: '--font-inder',
});

export const INTER = Inter({
  subsets: ['latin'],
  weight: '400',
  style: 'normal',
  variable: '--font-inter',
});
